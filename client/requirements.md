## Packages
framer-motion | Smooth animations for cart updates and page entry
clsx | Utility for conditional class names (standard in shadcn-like setups)
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
